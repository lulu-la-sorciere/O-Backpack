{% extends 'base.html.twig' %}

{% block subtitle %}
	- Pays
{% endblock %}

{% block content %}

	<div class="imageandtitle">
		<img src="#" alt="image monde" class="continentspec">
		<h2>{{continent.name}}
			- Choisissez un pays</h2>
	</div>
	<button class="continue post">
        <a href="{{ path('location_continent') }}">Retour à la liste des continents</a>
    </button>
	<ul class="continentul">
		{%  for data in datas %}
			<li class="continentli">
				<a href="{{ path('location_country',{id:id}, {name:data.name}) }}">{{data.name}}</a>
			</li>
		{% endfor %}
	</ul>
{% endblock %}
