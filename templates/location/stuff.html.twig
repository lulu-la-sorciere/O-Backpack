{% extends "base.html.twig" %}

{% block subtitle %}  Matériel  {% endblock %}

{% block content %}
    <div class="imageandtitle">
        <img src="#" alt="image monde" class="continentspec">
        <h2>{{country.name}} - Matériel</h2>
    </div>

    <div>
        <h3><a href="{{ path('location_country_administratif', {id:country.id}) }}">Administratif</a></h3>
        <h3><a href=""> Météo</a></h3>
    </div>

    <div>Matériel nécessaire :  </div>
    <ul>
        {% for stuff in stuffs %}

            <li> {{ stuff.name }} </li>
    
        {% endfor %}       
    </ul>    

{% endblock %}